import { FrameColorDirective } from './frame-color.directive';

describe('FrameColorDirective', () => {
  it('should create an instance', () => {
    const directive = new FrameColorDirective();
    expect(directive).toBeTruthy();
  });
});
