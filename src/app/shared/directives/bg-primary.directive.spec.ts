import { BgPrimaryDirective } from './bg-primary.directive';

describe('BgPrimaryDirective', () => {
  it('should create an instance', () => {
    const directive = new BgPrimaryDirective();
    expect(directive).toBeTruthy();
  });
});
